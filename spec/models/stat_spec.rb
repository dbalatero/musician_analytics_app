require 'spec_helper'

describe Stat do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    Stat.create!(@valid_attributes)
  end
end
